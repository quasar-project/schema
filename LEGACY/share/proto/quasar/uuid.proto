syntax = "proto3";

package quasar.pb;

/// UUID based on RFC 4122
message UUID {
  /// The string representation of the UUID
  bytes value = 1;

  /// The version of the UUID. Can be zero.
  uint32 version = 2;
}