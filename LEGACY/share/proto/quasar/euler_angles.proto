syntax = "proto3";

package quasar.pb;

/// Euler angles
message EulerAngles {
  /// Roll component in radians
  float roll = 1;

  /// Pitch component in radians
  float pitch = 2;

  /// Yaw component in radians
  float yaw = 3;
}
